import {styled} from '@fast-styles/react';
import {BottomSheetModalProvider} from '@gorhom/bottom-sheet';

const BottomSheetModalProviderFlex = styled(BottomSheetModalProvider, {
  flex: 1,
});

export {BottomSheetModalProviderFlex as BottomSheetModalProvider};
