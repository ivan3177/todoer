import {createTheme} from '@fast-styles/react';

export const appTheme = createTheme({
  colors: {
    $volcanicPearl: '#F0EDED',
    $cherryFrosting: '#EBE0E1',
    $lunarBlue: '#E0E6EB',
    $redstone: '#E46B71',
    $darkSlateBlue: '#214761',
    $knightRider: '#0F0707',
  },
});
